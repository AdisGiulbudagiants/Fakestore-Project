import { createSlice } from '@reduxjs/toolkit'

const initialState = {}

const clothesPageSlice = createSlice({
  name: 'clothes',
  initialState,
  reducers: {},
})

export default clothesPageSlice.reducer
